package controllers.converter;


public class HashMapToMapEntryAdapterConverter {

   /* public synchronized static List<MapEntryAdapter> convertHashMapToMapEntryAdapter(HashMap<String, Double> map) {
        List<MapEntryAdapter> list = new ArrayList<>();
        map.forEach((k, v) -> {
            MapEntryAdapter mae = new MapEntryAdapter();
            mae.setKey(k);
            mae.setValue(v);
            list.add(mae);
        });

        return list;
    }
*/
  /*  public synchronized static HashMap<String,Double> convertMapEntryAdapterToHashMap(List<MapEntryAdapter> list) {
        HashMap<String,Double> map = new HashMap<>();
        if(list.size()>0) {
            for (MapEntryAdapter mapEntryAdapter : list) {
                map.put(mapEntryAdapter.getKey(), mapEntryAdapter.getValue());
            }
        }
        return map;
    } */
}
